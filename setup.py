from setuptools import setup, find_packages

setup(
    name='your_project',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        'scikit-learn==1.5.0',
        # Other dependencies
    ],
)
